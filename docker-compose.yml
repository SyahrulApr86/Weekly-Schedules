services:
  app:
    container_name: wiiks-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:80"
    environment:
      - NODE_ENV=production
    networks:
      - traefik-network

networks:
  traefik-network:
    name: traefik-network
    driver: bridge